import { NextPage } from 'next'
import classnames from 'classnames'

interface Props {
  className?: string
  userAgent?: string
}

const Page: NextPage<Props> = () => {
  return <main></main>
}
export default Page
