overwrite: true
schema: "http://localhost:4000"
documents:
  - "src/queries/*.ts"
generates:
  src/types/fragment.json:
    plugins:
      - fragment-matcher
  src/types/queries.ts:
    plugins:
      - typescript
      - typescript-operations
